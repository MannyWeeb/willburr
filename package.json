{
  "name": "willburr",
  "version": "1.1.0",
  "description": "A small CLI application which allows users to share contents and materials over a network.",
  "main": "src/index.js",
  "scripts": {
    "console" : "node --experimental-modules --experimental-json-modules --unhandled-rejections=strict src/index.mjs --console",
    "console-dev" : "nodemon --config nodemon.json --experimental-modules --experimentason-modules --unhandled-rejections=strict src/index.mjs --console",
    "web" : "node --experimental-modules --experimental-json-modules --unhandled-rejections=strict src/index.mjs --web",
    "web-dev" : "nodemon --config nodemon.json --experimental-modules --experimental-json-modules --unhandled-rejections=strict src/index.mjs --web"
  },
  "author" : "Emmanuel A.",
  "license" : "ISC",
  "devDependencies" : {
    "nodemon" : "^2.0.4"
  },
  "resolveJsonModule": true,
  "dependencies" : {
    "chalk" : "*",
    "cors" : "*",
    "express" : "^4.17.1",
    "ffmetadata" : "^1.7.0",
    "prompt-sync" : "^4.2.0",
    "thumbsupply" : "^0.4.0"
  }
}
